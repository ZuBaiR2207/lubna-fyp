<%- include('../partials/header', { title: 'My Courses - SLMS' }) %>

<div class="container">
    <div class="card">
        <div class="card-header">
            <h2>My Courses</h2>
        </div>

        <% if (courses.length === 0) { %>
            <p>You are not enrolled in any courses yet.</p>
        <% } else { %>
            <table>
                <thead>
                    <tr>
                        <th>Course Title</th>
                        <th>Level</th>
                        <th>Teacher</th>
                        <th>Description</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% courses.forEach(course => { %>
                        <tr>
                            <td><strong><%= course.title %></strong></td>
                            <td><span class="badge badge-info"><%= primaryLevelLabel(course.primary_level) %></span></td>
                            <td><%= course.teacher_name %></td>
                            <td><%= course.description || 'No description' %></td>
                            <td>
                                <a href="/student/courses/<%= course.id %>" class="btn btn-sm btn-primary">View</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } %>
    </div>
</div>

<%- include('../partials/footer') %>
